# Flight control SDK
mavsdk==2.8.1

# Math and arrays
numpy>=1.24.0
scipy>=1.11.0

# Computer vision for perception layer
opencv-python>=4.8.0
ultralytics>=8.1.0

# Machine learning
torch>=2.1.0
torchvision>=0.16.0

# Real-time Telemetry and Dashboards
websockets>=12.0

# Async file I/O
aiofiles>=23.0

# Hardware Integration (Linux/Jetson only — install manually on target)
# smbus2>=0.4.3
# pyserial>=3.5

# Testing
pytest>=7.0
pytest-asyncio>=0.21

# ROS 2 Python client (optional for strict SITL, needed later)
# rclpy is typically installed system-wide with ROS2, so we don't pin it here.
